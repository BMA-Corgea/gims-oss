"""Unified Part-of-Speech model (the Phase 3 cohesion core).

One data shape, one spec, one handler/descriptor, one registry, one validation engine for
the six parts of speech. See ``handoff.md`` and ``proposals/gims_project_refactor.md`` (Phase 3)
for the design and the safe, incremental migration order.
"""
